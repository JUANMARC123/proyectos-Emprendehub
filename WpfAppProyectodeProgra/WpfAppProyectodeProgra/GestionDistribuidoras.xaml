<Window x:Class="WpfAppProyectodeProgra.GestionDistribuidoras"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gestión de Distribuidoras" Height="520" Width="950">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- TÍTULO -->
        <TextBlock Text="Gestión de Distribuidoras"
                   FontSize="28"
                   HorizontalAlignment="Center"
                   Margin="0 0 0 20"
                   FontWeight="Bold"
                   Foreground="Black"/>

        <!-- FILTROS -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 10">

            <!-- Filtro por texto -->
            <StackPanel Margin="10">
                <TextBlock Text="Buscar distribuidora:" Foreground="Black" />
                <TextBox x:Name="txtBuscar"
                         Width="300"
                         Height="30"
                         VerticalContentAlignment="Center"
                         TextChanged="txtBuscar_TextChanged"/>
            </StackPanel>

            <!-- Filtro Categoría -->
            <StackPanel Margin="10">
                <TextBlock Text="Categoría:" Foreground="Black" />
                <ComboBox x:Name="cmbFiltrarCategoria"
                          Width="200"
                          SelectionChanged="cmbFiltrarCategoria_SelectionChanged">
                    <ComboBoxItem Content="Todas" IsSelected="True"/>
                    <ComboBoxItem Content="Bebidas"/>
                    <ComboBoxItem Content="Ropa"/>
                    <ComboBoxItem Content="Tecnología"/>
                    <ComboBoxItem Content="Alimentos"/>
                </ComboBox>
            </StackPanel>

        </StackPanel>

        <!-- TABLA -->
        <DataGrid x:Name="dgDistribuidoras"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  Height="300"
                  Margin="0 10">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                <DataGridTextColumn Header="Ciudad" Binding="{Binding Ciudad}" Width="*"/>
                <DataGridTextColumn Header="Productos" Binding="{Binding Productos}" Width="2*"/>
                <DataGridTextColumn Header="Categoría" Binding="{Binding Categoria}" Width="*"/>
            </DataGrid.Columns>

        </DataGrid>

        <!-- BOTONES -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0 20">

            <Button Content="Ver Detalles" Width="150" Margin="10" Click="BtnDetalles_Click"/>
            
            <Button Content="Eliminar" Width="150" Margin="10" Click="BtnEliminar_Click"/>

        </StackPanel>

    </Grid>
</Window>
