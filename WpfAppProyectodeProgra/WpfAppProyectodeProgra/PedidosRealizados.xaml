<Window x:Class="WpfAppProyectodeProgra.PedidosRealizados"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pedidos Realizados" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0 0 0 10">
            <TextBox x:Name="SearchBox" Width="400" VerticalContentAlignment="Center" Padding="5" TextChanged="SearchBox_TextChanged"/>
            <Button Content="Buscar" Width="80" Margin="5 0 0 0"/>
        </DockPanel>

        <DataGrid x:Name="PedidosGrid" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                <DataGridTextColumn Header="Ciudad" Binding="{Binding Ciudad}" Width="*"/>
                <DataGridTextColumn Header="Productos" Binding="{Binding Productos}" Width="*"/>
                <DataGridTextColumn Header="Categoría" Binding="{Binding Categoria}" Width="*"/>
                <DataGridTextColumn Header="Fecha Pedido" Binding="{Binding Fecha}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0 5 0 0">
            <Button Content="Eliminar" Width="100" Margin="5" Click="BtnEliminar_Click"/>
            <Button Content="Confirmar" Width="100" Margin="5" Click="BtnConfirmar_Click"/>
        </StackPanel>
    </Grid>
</Window>
